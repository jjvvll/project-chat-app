-Update composer
-Generate env file
-Generate app key
-run migrations
-install reverb, then run (php artisan reverb:start)
-install imagick (download the one matching to your php version, copy the php_imagick.dll file to xampp>php>ext then edit php.ini and add extension=php_imagick.dll )
-run php artisan storage:link
